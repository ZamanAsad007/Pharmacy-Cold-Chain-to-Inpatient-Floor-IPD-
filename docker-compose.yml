services:
  hapi-fhir:
    image: docker.io/hapiproject/hapi:v6.10.0
    container_name: hapi-fhir
    ports:
      - "8088:8080"
    environment:
      - hapi.fhir.fhir_version=R4
    restart: unless-stopped
