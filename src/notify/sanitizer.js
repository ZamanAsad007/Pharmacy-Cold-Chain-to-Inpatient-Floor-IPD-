/**
 * PHI Sanitizer and Nurse Notification module (Phase 5 stub)
 */
function sanitizeForNurseNotification(dispenseRecord) {
  // To be implemented in Phase 5
  return { status: 'pending' };
}

module.exports = {
  sanitizeForNurseNotification
};
